name: ramsey-db

services:
  db:
    image: mysql:9.5
    container_name: ramsey-db-mysql
    networks:
      - ramsey-net
    ports:
      - "36001:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - ./config-file.cnf:/etc/mysql/conf.d/config-file.cnf
      - /Users/benferenchak/Ramsey/Database/ramsey-db/data:/var/lib/mysql
      - /Users/benferenchak/Ramsey/Database/ramsey-db/tmp:/var/lib/mysql/tmp
    restart: on-failure

networks:
  ramsey-net:
    driver: bridge
    name: ramsey-net